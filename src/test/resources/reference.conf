lease-management {
  # lease timeout in seconds - 10 mins
  stale-lease-delta = 600
  commit {
    aws {
      region = "eu-west-1"
      dynamodb {
        read-capacity-units = 5
        write-capacity-units = 6
      }
    }
  }
}
